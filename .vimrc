runtime! dein/*.vim
runtime! config/*.vim
runtime! config/plugins/*.vim

filetype plugin indent on

"Note:
" neovim設定
"   .vim -> ~/.config/nvim へリンク
"   .vimrc -> ~/.config/nvim/init.vimへリンク
"   (init.vimは.vimディレクトリ内にシンボリックリンクファイルが作成される）

